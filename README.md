# ImGui Grid Experiment

Goal: create a table which autosizes and assigns the same height and width to each cell,
optional bonus if they're rectangular.

# Instructions

Use cmake to configure and build:

``` bash
$ cmake -G Ninja -B build .
$ cmake --build build
```

